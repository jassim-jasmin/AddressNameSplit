import mysql.connector
from DB import sqlDB

select id from table where fieldName like 'c/o,attn'

def merge(schemaName, tableName, arrayOfFields):
    select id,arrayOfFields[0] from tabelName where arrayOfFields[0]has careofname and arrayOfFields[i].address is null

    for i in range(1,len(arrayOfFields)):
        if check_co[arrayOfFields[0]:
            if arrayOfFields[1].address is null and arrayOfFields.name is not null:
                then concat arrayOfFields[0],arrayOfFields[i]

def fixCareOfNameFields(tableName, fields):
    firstField = tableName.pop()
    for secondField in fields:
        idDataList = sqlDB.getConcatId(sqlDB,tableName, idFieldName + '_name_extract', firstField + '_name_extract', secondField, idList)
        sqlDB.replaceRow(sqlDB, tableName, idField, firstFieldName + '_name_extract', secondField + '_name_extract', idDataList)